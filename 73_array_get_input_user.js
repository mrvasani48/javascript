//get use total length of array
var arr= prompt("input array length ",5);

// declaration array
var array=[];

// intilizing array and  print array  value 
for (let i=0;i<arr;i++ )
{
    array[i]=prompt("enter the array");
    document.write(array[i]+" <br>");
};

